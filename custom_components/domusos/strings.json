{
  "title": "Domus UI",
  "config": {
    "step": {
      "user": {
        "title": "Set up Domus UI",
        "description": "Add Domus UI to the sidebar. It uses the current Home Assistant account for identity, permissions, and commands."
      }
    },
    "abort": {
      "single_instance_allowed": "Domus UI is already configured."
    }
  }
}
